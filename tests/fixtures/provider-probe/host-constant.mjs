// MUTATION FIXTURE — the host lives in ANOTHER module and is imported.
// The importing module contains no host literal at all.
export const MARKET_HOST = 'https://api.imported-const-vendor.example';
